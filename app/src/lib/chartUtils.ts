/**
 * @file lib/chartUtils.ts
 * Backward-compatible re-export barrel.
 * Prefer importing directly from @/lib/formatters.
 */
export { dateLabel, formatLargeUSD, seededRand, thinData } from "@/lib/formatters";
